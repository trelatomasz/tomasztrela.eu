{
  "name": "tomasztrela-eu",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "clean": "rimraf dist",
    "dev": "astro dev",
    "start": "astro dev",
    "prebuild": "node scripts/generate-diagrams-simple.js",
    "build": "npm run prebuild && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "gh-pages -d dist",
    "generate-diagrams": "node scripts/generate-diagrams-simple.js",
    "setup-diagrams": "node scripts/setup-diagrams.js"
  },
  "dependencies": {
    "astro": "^5.12.8",
    "mermaid": "^11.9.0"
  },
  "devDependencies": {
    "@astrojs/tailwind": "^6.0.2",
    "@mermaid-js/mermaid-cli": "^11.0.0",
    "@tailwindcss/typography": "^0.5.16",
    "gh-pages": "^6.3.0",
    "rimraf": "^6.0.1",
    "tailwindcss": "^3.3.0"
  }
}
